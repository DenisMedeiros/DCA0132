#!/usr/bin/env python
import sys
# Read each line from stdin
for line in sys.stdin:
  # Get the words in each line
  words = line.split(" ")
  # Generate the count for each word
  
  

'''    
  if 'user=' in words[7]:
    email = words[7].replace("user=<", "").replace(">,", "")
    if '@' in email:
        username = email.split("@")[0]
        if '.' in username:
            clean_username = username.split(".")[0]
            print '{0}\t{1}'.format(clean_username, 1)
    else:
        if '.' in email:
            clean_username = email.split(".")[0]
            print '{0}\t{1}'.format(clean_username, 1)
        else:
            print '{0}\t{1}'.format(email, 1)
'''            
          
